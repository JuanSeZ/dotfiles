- hosts: localhost
  tasks:
  - name: Install tmux
    become: false  
    community.general.homebrew:
      name: "tmux"
      state: present

  - name: Install tpm
    ansible.builtin.git:
      repo: https://github.com/tmux-plugins/tpm
      dest: ~/.tmux/plugins/tpm
